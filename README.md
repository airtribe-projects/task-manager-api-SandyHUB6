# Task Manager API

A RESTful API built with Node.js and Express.js for managing tasks using in-memory storage.

## Installation

npm install

## Run Server

node app.js

## Run Tests

npm test

## Endpoints

GET /tasks
GET /tasks/:id
POST /tasks
PUT /tasks/:id
DELETE /tasks/:id